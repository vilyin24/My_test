Для успешного запуска автотестов необходимо:
1) Установить Python версии не ниже 3. У меня установлена 3.13.1 (можно проверить в терминалке
командой: python --version)
2) Установить библиотеки (описание библиотек ниже)
3) Проверить, что создана преапрувнутая заявка на тестовом пользаке (в моем случае все тесты на пользаке utes321)
4) Все тестовые данные( brokerId, biztalkId, jobId ) хранятся в директории data -> data_test.
При необходимости нужно вставить свои данные по заявке и biztalkId
5) Для запуска автотестов нужно в терминалке команду: pytest -v -s (запустятся все тесты в разноброс,
лучше использовать команду, которую прикреплю ниже)
6) После того, как автотесты прошли, сгенерировать отчёт Allure командой в терминалке: allure serve allure-results
7) Если автотесты упали, попробовать вызвать какой-нибудь запрос через Swagger
8) В случае, если завершился ошибкой только один тест, посмотреть лог ошибки

Рекомендация: Перед запуском тестов лучше добавить пользаку 3-4 работы

Формирование отчёта Allure:
1) Запуск автотестов командой ниже
2) Ввести команду: allure serve allure-results в терминалке
3) После просмотра результатов рекомендую удалить папку allure-results в проекте

Команда в терминалке для запуска автотестов в правильном порядке:
pytest v3_parameters/test v3_personal_data_init_bdf_screen/test v3_personal_data_update_init_bdf_screen/test v3_personal_data_update_bdf_screen/test v3_get_jobs/test v3_jobs_init_bdf_screen/test v3_jobs_create/test v3_jobs_update/test v3_delete_jobs/test --alluredir=allure-results

Необходимые библиотеки для проекта:
1. Попробовать установить зависимости одной командой: pip install -r requirements.txt

Если не получилось установить, то вручную установить эти зависимости:
1. pytest (pip3 install pytest)
2. request (pip3 install request)
3. allure-pytest (pip3 install allure-pytest)
4. jsonschema (pip install jsonschema)

